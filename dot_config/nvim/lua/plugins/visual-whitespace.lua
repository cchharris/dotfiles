return {
    'mcauley-penney/visual-whitespace.nvim',
    enabled = false, -- Needs a patch for getregionpos
    opts = {},
    keys = { 'v', 'V', '<C-v>' }, -- optionally, lazy load on visual mode keys
}
